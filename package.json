{
  "name": "rookery-crypto",
  "version": "0.1.0",
  "description": "Browser-side PGP crypto module for rookery. Bundled by esbuild inside the Containerfile build; never run with npm in production.",
  "private": true,
  "scripts": {
    "build": "esbuild --bundle --minify --format=iife --global-name=RookeryCrypto --outfile=./static/crypto.js ./client/index.js"
  },
  "devDependencies": {
    "esbuild": "0.25.5"
  },
  "dependencies": {
    "openpgp": "6.1.1"
  }
}

# Generated secrets — never commit
.env

# Operator config — contains instance-specific values
rookery.toml
Caddyfile

# Go build output
rookery-server

# Go test cache and coverage
*.test
*.out

# Node (esbuild is installed locally for editor tooling only;
# the real build happens inside the Containerfile)
web/crypto/node_modules/
